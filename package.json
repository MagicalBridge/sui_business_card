{
  "name": "sui-business-card",
  "version": "0.1.0",
  "description": "TypeScript tests for Sui Business Card smart contract",
  "scripts": {
    "test": "jest --watch",
    "build": "tsc",
    "dev": "ts-node",
    "deploy": "ts-node scripts/deploy.ts",
    "deploy:testnet": "ts-node scripts/deploy.ts testnet",
    "deploy:devnet": "ts-node scripts/deploy.ts devnet"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.1.0",
    "jest": "^30.0.5",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@mysten/sui": "^1.36.2",
    "dotenv": "^17.2.1"
  }
}
